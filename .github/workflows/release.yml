name: 🚀 Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: DownAtTheBottomOfTheMoleHole/.github/.github/workflows/auto-release.yml@b3189cd9f29bed2093dcf7dd5616ed1b5ba1be98 # main
    secrets: inherit
    permissions:
      contents: write
