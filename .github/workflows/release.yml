name: 🚀 Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: DownAtTheBottomOfTheMoleHole/.github/.github/workflows/auto-release.yml@30061998c92acd96f266ab2f81392dc51ebffdbb # main
    secrets: inherit
    permissions:
      contents: write
