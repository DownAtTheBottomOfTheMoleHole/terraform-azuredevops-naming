name: 🚀 Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: DownAtTheBottomOfTheMoleHole/.github/.github/workflows/auto-release.yml@d1a28d3124a84878bc94ca5791ca9d643777592d # main
    secrets: inherit
    permissions:
      contents: write
