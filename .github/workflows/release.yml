name: 🚀 Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: DownAtTheBottomOfTheMoleHole/.github/.github/workflows/auto-release.yml@b0fc23679567bf6024c32154c8364aab32f6e98e # main
    secrets: inherit
    permissions:
      contents: write
